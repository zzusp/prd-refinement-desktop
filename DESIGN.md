---
version: alpha
name: "需求显微镜"
description: "面向产品、研发和测试的 PRD 需求细化桌面工作台，以来源覆盖轨道呈现需求证据链。"
colors:
  ink: "#172027"
  navy: "#17324A"
  primary: "#246DA8"
  evidence: "#5EB8BD"
  warning: "#D69635"
  paper: "#F7F9FA"
  border: "#CCD5DA"
typography:
  sans:
    fontFamily: "Microsoft YaHei UI, Noto Sans SC, system-ui, sans-serif"
  mono:
    fontFamily: "Cascadia Mono, ui-monospace, monospace"
rounded:
  DEFAULT: "0.5rem"
  sm: "0.25rem"
  md: "0.5rem"
  lg: "0.75rem"
spacing:
  panel: "1rem"
  section: "1.5rem"
components:
  projectRail: {}
  sourceTrack: {}
  requirementList: {}
  sourceDrawer: {}
---

# 需求显微镜 Design System

## Overview

创意参照是“云平台任务控制台”：上传页只引导一个主动作，任务详情用紧凑状态栏和一条连续阅读轴承载范围、结果与执行记录。用户是需要把大 PRD 交给 Agent 的产品、研发和测试人员，主要场景是 macOS 和 Windows 桌面端的长时间审阅。

界面使用中文。产品层级和状态应安静、清楚；唯一强调元素是青色来源轨道。避免聊天窗口、通用 AI 渐变、营销式大数字和大面积卡片堆叠。本文是设计令牌与意图的权威来源，运行时映射位于 `src/styles.css` 的 `:root`。

运行时设置默认同时执行 5 个任务，每个任务最多并发 10 个分析节点；设置页展示的取值范围必须与调度器实际限制一致。

单任务并发是模型调用的硬上限，不能只影响批次切分。执行成本区使用横向执行账单：左侧突出总耗时和模型活跃耗时，右侧紧凑排列调用、排队、重试和提示词估算，底行统一显示 Runtime Token；不使用自动换行的卡片矩阵。缺失指标明确写“Runtime 未返回”。

执行记录保留七阶段纵向轨迹，每个已产生可靠结果的阶段在节点末尾显示一行简短产出摘要。执行中使用蓝色弱底并标记“当前产出”，完成后使用绿色弱底并标记“产出”；摘要使用业务数量和结果，不展开完整清单。

## Colors

深海军蓝用于项目导航，蓝色表示当前操作，青色表示来源、证据和完整处理。平台错误使用红色，并配合文字标签。背景使用冷白和浅灰，保证长时间阅读中的层级稳定。

## Typography

中文正文使用 Microsoft YaHei UI 与 Noto Sans SC 回退；需求编号使用 Cascadia Mono。内部来源编号不出现在业务阅读界面。标题保持紧凑，正文行高增加，以支持原文的持续阅读。

## Layout

桌面应用以任务中心为平台入口。任务详情统一为全宽工作台：紧凑标题栏集中版本、交付、打开产物和任务管理；摘要行显示执行状态、本期和本期不做，右侧以“耗时/用量”入口承载任务级统计并默认折叠，展开后直接显示完整节点成本分布，不设置二级折叠；该统计不在执行记录中重复出现。主体使用功能与需求、全部需求、资料包、执行记录四个视图。“功能与需求”“全部需求”和“资料包”的总数以 Tab 后的徽章展示，顶层列表不再重复显示标题计数行；进入单个功能的需求明细时仍保留上下文标题。资料包视图只读呈现任务启动时冻结的文件、角色、读取状态与本次分析输入，不读取后来变化的原资料包，也不复制资料准备页的编辑操作。执行图的节点耗时与节点名称位于同一标题行，说明、模型信息和产出摘要使用完整内容宽度，不为耗时保留贯穿节点高度的空列。完成任务默认打开功能与需求，执行中任务默认打开执行记录。各区域服从同一 1320px 阅读轴，最低宽度为 1100px。

Electron 使用操作系统原生标题栏承载窗口控件，应用顶部栏始终保持同一套 26px 左右内边距，不按平台移动 Logo 或导航。

顶部栏右侧以弱化文字持续显示当前安装版本，Runtime 状态仍是主要环境状态。Runtime 配置页提供独立的应用版本卡片；远端检查必须由用户主动触发，并区分检查中、已是最新、发现新版本和检查失败。发现新版只打开固定的官方 GitHub Releases 页面，不在应用内静默下载或安装。

任务列表以“任务编号 + 项目名称”为第一扫描层，来源文件降为第二层；状态、阶段、进度和时间使用稳定列宽。任务行不得低于 84px，避免三层任务信息与运行指标互相挤压；整行作为进入详情的唯一操作，并提供 hover 与键盘焦点反馈。

任务状态只描述执行生命周期，使用“排队中 / 执行中 / 已完成 / 执行失败”。平台执行或引用校验错误在执行记录中展示，并影响交付资格。

交付包随初次分析、结果调整和本期范围新版本自动生成。任务页正常状态只显示“打开产物”；仅当完成结果的产物缺失时显示次要恢复操作“重新生成产物”，不常驻显示生成按钮。

功能与需求列表、全部需求列表都提供批量范围操作。复选框只表达本次操作选择，独立的“本期 / 本期不做 / 部分纳入”标签显示已保存范围。当前功能、内容搜索、本期范围和选择范围统一收进一条筛选栏；批量操作栏仅在已有选择或需要反馈时出现，提供标记本期不做、恢复本期和清除选择。筛选变化会清空隐藏选择并提示。从功能列表进入需求列表时，当前功能以可清除的筛选条件显示在同一控制栏内，不再独占一层横栏，也不重复渲染需求页标题。

完成任务的顶部操作区提供“调整结果”次要按钮，点击后在任务摘要与结果视图之间展开任务级多行输入区。用户可一次描述跨功能的模块组织和需求颗粒度调整；输入区跨结果视图共享草稿，收起不清空，失败时保留输入。




任务中心分为当前任务和已归档任务。归档与恢复保留完整任务族；删除使用应用内警告对话框，先聚焦“保留任务”，显示任务名、真实版本数和删除范围。运行中的归档或删除按钮明确说明会先停止执行，所有失败在当前行或任务页就地显示。

平台生成清单与原文不一致的内部校验过程不进入用户结果页，也不形成用户待办。真实任务执行失败仍通过执行记录中的失败状态和错误信息反馈。局部复核分别记录原目标、候选回归和旁支发现。

任务进度只表达执行阶段进展，所有阶段成功结束即显示“已完成 · 100%”，不使用任务状态或中间百分比编码交付质量。真实执行失败显示错误与恢复操作；正式包或草稿仅由独立产物状态决定。

## Elevation & Depth

依靠色块、分隔线和固定栏位表达层级。静态卡片不使用阴影；焦点与选中状态用描边和内侧标记表达。

## Shapes

操作控件与信息容器使用 8–12px 圆角；来源轨道节点保持圆形。避免胶囊形状成为通用装饰。

## Components

按钮、搜索框和可选行均提供 `focus-visible`。加载保持原布局；失败信息必须说明失败对象和可恢复动作。图标来自同一线性图标族，主要操作保留文字标签。动效只用于阶段状态变化，并尊重 reduced motion。

## Do's and Don'ts

- Do：需求内容只显示模块、简短需求项、原文入口与本期范围，不展示旧版逐条检查状态，也不拆成条件、限制、验收条件字段。
- Do：使用固定编号和稳定列宽帮助高密度扫描。
- Don't：将 Agent 对话作为主界面或让聊天记录替代结构化产物。
- Don't：用“覆盖率 100%”掩盖未读材料或未解决的语义问题。



正文与表格使用 12–13px 基线，避免原有 8–9px 小字号；明细行保留足够行高，来源抽屉以 13px 展示文本。

## 资料包准备

新建任务由 `src/MaterialWorkspace.tsx` 统一承接。入口沿用原 PRD 上传页的单一主动作；选择主 PRD 后切换到“准备本次分析”，在同一阅读轴依次展示主 PRD、可选补充资料、默认展开的说明区和“开始分析”。索引属于平台内部准备步骤，只在运行时显示真实文件进度，不作为用户必须单独触发的动作。

初始页采用 760px 单一阅读轴，依次显示三步流程、主 PRD 选择卡和已有资料恢复入口。主 PRD 选择是唯一主操作；已有资料恢复为卡片下方的次要入口。首次文件选择期间只在内容轴内显示加载反馈，不出现无法执行的取消按钮。

已有资料包的“重命名”和“删除资料包”直接显示在恢复选择器旁，不再经过通用管理入口。重命名显式保存；整体删除使用页面内二次确认，并在确认文案中说明删除范围和原始文件不受影响。资料处理中禁用这两个动作。

选中主 PRD 后切换为左对齐的资料准备工作态，内容宽度统一为 1040px。视觉顺序固定为主 PRD 身份、可选补充资料、补充说明与调整、开始分析和折叠的资料明细。说明区是普通表单而非聊天窗口，默认六行并显示本机保存状态；“开始分析”是唯一主按钮。系统只读取用户上传的文件，不显示或判断缺件。

准备页与结果页共用“用户一次描述、平台统一处理”的交互语义。输入区只提交用户说明，不生成或采纳建议。任务详情在“资料包”视图展示本次分析输入和用户原话，不显示内部来源编号。

“本次分析输入”同时展示平台逐条应用结果，使用业务语言说明该条是业务补充、范围决定、整理要求、待回答疑问或替换口径，并显示影响数量。内部来源编号、指纹和调度字段不进入用户界面。

## 检查清单交付


保留七阶段、九个模型配置和统一调整入口。业务结果仅显示功能模块与简短需求及原文，不展示内部清单校验过程，也不生成待处理事项、建议、主张或独立条件栏目。Agent 包以 agent-checklist.md 作为唯一实施清单，只附原始资料、简短 README 和完整性 manifest；共同原文位置在模块下集中显示，特殊来源随对应条目显示，需求内容不做语义合并。实现时必须先读 PRD，再用清单查漏。

# 分析执行根因修复

## 总目标

取消平台 token 限制，并使模型工作失败可准确恢复：失败不能伪装成审计结果或业务阻塞，重试只复用真实成功的工作。

## Sub goal matrix

| 子目标 | 状态 | 完成证据 |
| --- | --- | --- |
| 移除所有平台 token 拒绝、阈值拆批与累计限制 | 完成 | 25,000 token 单元测试及超过旧上限的调度请求到达模拟 Runtime |
| 保留请求与实际用量统计 | 完成 | PromptMetric 和调度检查点回读 |
| audit 按显式工作状态持久化并恢复 | 完成 | 失败功能调用 2 次，成功功能保持 1 次 |
| 执行故障不进入业务审计问题 | 完成 | audit 故障时 AuditIssue 为空，ExecutionFailure 有记录 |
| 所有当前模型入口无 token 门禁 | 完成 | 源码搜索无上限、断言和 token 比较分支 |
| 真实桌面任务验证 | 部分完成 | pipeline 20 创建并真实调用 43 次；在候选重分类阶段因独立语义问题失败，未到 audit |

## 重大决策

- Pipeline 协议升级，旧的失败结果数组不继续作为新执行证明。
- token 估算仅用于指标，不参与调用资格或分片。
- 超时、并发、取消与有界格式纠错继续保留。

## 重要信息

- 真实任务 `T-14BAB15B` 的 F-001 audit 请求估算为 21,321 tokens，旧平台限制为 16,000；请求未到达 Runtime。
- 用户原有的其他未跟踪 spec 文件不属于本次提交。
- 真实恢复任务为 `T-77F83164`。它没有出现 token 门禁，在功能候选重分类两轮后因来源角色判定未收敛而失败；该问题不属于本次 token/恢复修复，不能算作 audit 通过。

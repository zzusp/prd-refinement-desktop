# 类型化节点执行

> 状态：ACTIVE
> Goal ID：typed-node-execution
> 最近维护：2026-09-14T16:15:00+08:00
> 权威目标：D:/project/prd-refinement-desktop/docs/acceptance/typed-node-execution/goal.md

## 总目标

实施 docs/spec/typed-node-execution-contract.md：所有可达模型节点具有输入、候选和正式结果契约，统一执行、错误分类及批次恢复。

## 完成条件

- 全入口迁移、故障注入、类型检查、测试和构建通过。
- 原失败节点回归、真实任务与实际 Electron 验证分别留证。
- 文档同步，PR 独立回查；未验证边界不宣称完成。

## 范围与约束

- 保留 Codex 登录与 Runtime 配置，不加 token 门禁，不改三个既有未跟踪设计文件。
- 旧任务保持原始历史，新协议创建新任务。

## sub goal matrix

| ID | 子目标 | 完成判据 | 状态 | 证据 |
| --- | --- | --- | --- | --- |
| SG1 | Runtime 类型化操作 | 必需 Schema、完成事件、取消和分类测试 | 已完成 | Runtime 31 项测试；原生全量13/13及最新增量2/2通过 |
| SG2 | 领域契约 | 全可达候选及输入结构，Schema 同源 | 已完成 | 13 契约，10契约测试；原批次Terra一次真实调用验收通过 |
| SG3 | 执行器和调度集成 | 有界修正、成功回执、批次恢复、全入口迁移 | 已完成 | 全量257测试；3批恢复、同单元并发提交、不同输入操作ID、类型化领域异常均通过 |
| SG4 | 验证及交付 | 自动测试、真实回归、桌面、PR | 进行中 | round-4全任务与实际桌面通过；交付文件独立读取及PR收尾中 |

## 当前检查点

- 当前子目标：SG4
- 唯一下一步：完成交付文件独立回读并更新 PR。
- 未闭环项：交付文件独立回读及 PR；原 Luna 容量并未恢复，不计为原模型通过。

## 进展

- 2026-09-14：用户批准方案实施；读取方案和当前代码，开始分工。
- 2026-09-14：统一执行器、13 契约及全入口已接入；242 项首轮全测通过。原生探针拒绝 oneOf 和 optional required 子集，测试成功不代表原生调用通过，继续修正。
- 2026-09-14：原节点输入哈希精确复原；Luna容量失败单独保留。用户批准隔离使用Terra，原批次1调用生成5需求1澄清，通过全部确定性验收。全量257测试、构建和UI契约通过。首条全任务 T-9E6B2062 使用碰撞修复前构建；最终构建另起第四次桌面验证，不把前轮当最终版本证据。

## 重大决策

- 本文件是此次类型化节点重构的唯一权威目标；根 goal.md 和旧修复目标不记录本轮状态。
- 供应商子集采用语义等价转换（互斥标签 oneOf→anyOf、optional presence 分支）；不删除本地约束，不统一补 null，不恢复 token 门禁。
- 原用户配置和原任务不变；仅隔离验收显式将模型改为Terra。

## 重要信息

- 原失败任务 T-1F2FBCF6，须冻结节点输入而非按 F-003 名称替代。

# UX Contract

## Product context

- Audience：产品、研发、测试和需求评审人员。
- Primary jobs：导入 PRD，确认材料范围，审阅功能点，逐项检查简短需求和原文。平台不生成测试用例或验收场景。
- Active locales：zh-CN。
- Accessibility target：WCAG 2.2 AA。

## Business-context sources

| Domain / scope | Authoritative source | Source type | Reviewed date |
|---|---|---|---|
| 一期需求细化范围 | `D:\baibu-agent\docs\spec\prd-refinement-delivery-design.md` | Product spec | 2026-09-09 |

## Visual contract

- Project `DESIGN.md`：项目根目录 `DESIGN.md`。
- Token ownership：DESIGN.md 定义意图和值，`src/styles.css` 映射运行时 CSS 变量。
- Supported themes：一期只支持浅色。
- Electron window chrome：窗口控件由操作系统原生标题栏承载，不覆盖 Web 内容；应用顶部栏在各平台使用同一套布局。

## Canonical UI Map

| Capability | Canonical owner | Source of truth | Allowed variants | Verification |
|---|---|---|---|---|
| File upload | `src/MaterialWorkspace.tsx` | `electron/material-bundle.ts` | 主 PRD、补充文件和目录 | component + E2E |
| Analysis input | `src/MaterialWorkspace.tsx` 的“补充说明与调整”与任务页 `TaskFeedback` | `electron/material-bundle.ts`、`electron/scheduler-v2.ts` | 首次分析、结果调整 | unit + E2E |
| Search | `src/App.tsx` 的 `RequirementTable` | `src/App.tsx` | 需求明细本地过滤 | keyboard + E2E |
| Select/Listbox | native | `src/styles.css` | Runtime 配置、资料包恢复、资料用途和来源文件 | keyboard + E2E |
| Toast | 暂不适用 | 本契约 | 后续统一实现 | n/a |
| Scrollbar | `src/styles.css` | DESIGN.md | panel scroll | browser inspection |

## Flow ledger

| Operation | Trigger | Pending | Success destination | Failure recovery |
|---|---|---|---|---|
| 准备本次分析 | 选择定版 PRD，按需补充文件和说明 | 文件与说明分别保存并显示准确状态 | 留在准备页，可继续编辑 | 保留输入并显示具体保存或文件错误 |
| 开始分析 | 点击一次“开始分析” | 固定完整输入，自动读取资料、建立索引并创建任务 | 进入任务详情并继续执行 | 保留资料和说明，可处理具体问题后重试 |
| 搜索明细 | 输入查询词 | 即时本地过滤 | 当前列表 | 清空恢复全量 |
| 自动生成交付包 | 初次分析、结果调整或本期范围新版本形成 | 随结果版本一起生成，不要求用户再次操作 | 页内只显示“打开产物” | 产物缺失时显示“重新生成产物”恢复操作 |
| 打开产物 | 当前版本存在已生成交付包 | 只显示“正在打开” | 打开记录中的确切目录 | 保留已生成状态，单独显示打开失败 |
| 按说明调整结果 | 在任务级输入区一次描述模块组织或需求颗粒度调整 | 保存原始输入，定位受影响内容并排队生成一个新版本 | 打开新结果版本并显示落实情况 | 草稿和原始输入保留，可从检查点重试 |
| 批量调整本期范围 | 在功能或需求列表选择条目 | 显示选择数并禁止重复提交 | 生成新结果版本并清空选择 | 保留选择，在当前工具栏显示错误 |
| 归档/恢复任务 | 从任务更多菜单选择 | 运行中先停止，任务族串行保存 | 移入当前任务或已归档列表 | 当前行或任务页显示失败 |
| 删除任务 | 在应用内对话框确认 | 运行中先停止并锁定重复操作 | 返回当前任务列表 | 对话框保留并显示失败，可重试 |

## Async and resilience

- Harness 进程由 Electron 主进程持有；渲染进程不能直接启动程序或访问文件系统。
- 任务级调整输入在功能与需求、全部需求和执行记录之间共享；切换视图不得清空草稿。提交支持按钮和 Ctrl+Enter，空输入不创建任务。
- 每次分析保存输入 revision、attempt 和候选内容/功能级检查点；候选检查与返工预算同时保存。Runtime 进程退出不删除已验收结果。
- 取消分析时使当前 attempt 失效并结束 Runtime；迟到结果不能提交到已取消任务。
- 导入项目保存在 Electron userData 下，写入前保持结构完整。

## Verification

- Required static commands：`npm run typecheck`、`npm test`、`npm run build`、`npm run verify:premium`。
- Browser/device matrix：1440×900 和 1100×720，键盘焦点与长文本。
- Project audit：frontend-design-premium strict audit。

## Scale contract

- 单份 PRD 的目标规模为 20–100 个功能点、100–1000 个需求明细。
- 主路径固定为上传文件、查看执行过程、审阅结果汇总；不在单页同时展开全部层级。
- 功能与需求、全部需求和执行记录是任务详情的独立视图；单条详情仅在用户选择时打开。视图切换使用普通按钮，不声明未实现方向键模型的 ARIA tabs。
- Excel 是用户交付物，应用内部 JSON 只用于恢复任务和结果页面。
- 顶部“任务”是所有分析任务的统一入口；任务列表展示状态、当前阶段、进度、总耗时和创建时间。
- 多个运行中任务彼此独立推进；最大并行数用于真实 Harness 调度，超限任务进入排队。
- 执行详情的每个阶段显示完成耗时、当前执行时长或等待状态。
- 执行详情的每个非等待阶段可在节点末尾显示一行由现有检查点确定性计算的产出摘要；执行中标为“当前产出”，完成后标为“产出”，数据不足时不显示，不调用模型补写，也不展示内部校验事项。
- 执行详情的每个阶段按“职责、处理对象、模型、推理深度”逐项说明；多模型阶段必须解释各自分工，确定性脚本节点明确标记不使用模型。节点执行次数统一称为“累计业务调用”，不使用含义模糊的“轮”。
- Runtime 配置拥有独立页面，快速模型、关键模型、各自推理深度和可选网络代理的变更只影响新任务；连接检测使用当前表单中的代理值和主模型发送一次最小请求，不逐节点串行探测。
- 候选识别、用户补充理解和简单细化走快速模型；功能统一、复杂细化、产物依据核查和有据修正默认 Sol/low。
- 九个模型配置分别控制图片、用户补充理解、候选、候选返工、统一、简单细化、复杂细化、审计、局部修正；确定性脚本不显示模型配置。
- 节点成本分布按真实调用记录中的节点、模型和推理深度分别聚合；相同节点使用不同配置时不得合并为一行。
- Runtime 成本摘要采用固定两层布局：首层突出点击到结果和模型活跃，并紧凑显示调用、排队、重试及提示词估算；第二层显示输入、缓存输入和输出 Token。指标缺失时显示“Runtime 未返回”，不得形成残缺的自动换行网格。
- 候选识别和逐功能细化按独立对象并行；独立修正并行计算并在最新项目上串行提交。每任务硬上限及全局调用上限共同约束并发。
- Excel 需求清单使用七列检查表；每条需求只有一个主归属，跨功能关系在结果快照中保留。旧版检查点仅供查看，不能用新契约续跑。
- 失败任务提供“从检查点重试”；失败的调整任务显示已保存的调整输入。初次分析和调整任务都使用七阶段记录，终态不保留待执行步骤。逐功能细化和审计提供 1–10 的单任务节点并发设置。


完成结果默认进入功能与需求，执行中默认进入执行记录。来源处置保留在只读结果快照。范围状态持久化到结果版本，列表复选框和筛选仅暂存于页面；筛选变化清空隐藏选择。需求详情抽屉为非模态补充阅读区域，支持 Escape 关闭。

## 资料包工作流（2026-09-10）

- 资料创建、资料包重命名与删除、文件/目录选择、拖放、路径/角色编辑、排除与移除的唯一 UI 入口为 `src/MaterialWorkspace.tsx`，后端真值为 `window.prdApp.materials`。取代上述旧单文件上传流程。
- 新建任务首先显示原上传页式的单一主 PRD 选择卡片；首次选择时在后台创建资料包，不增加“先新建资料包”的前置步骤。主 PRD 保存成功后才展示补充资料、索引和来源管理。
- 初始页按“选择主 PRD、补充资料、识别并分析”展示三步路径；已有资料恢复位于主选择卡下方，不与主操作争夺注意力。首次选择文件时不得显示实际不可用的取消按钮。
- 选择一个主 PRD，补充文件可标记补充资料或历史参考；目录添加前可指定挂载路径，留空使用目录名，添加后可调整文件逻辑路径。
- 原生 select/input 沿用 Runtime 设置页控件；保存需要显式点击，成功由 IPC 返回后显示。输入失败保留当前编辑值；移除有内联确认，原始文件不删除。
- 选中已有资料包后，“重命名”和“删除资料包”直接显示在恢复选择器旁。重命名不改变内容 revision 或已完成索引；删除资料包必须内联二次确认，清理保存的快照、索引和缓存并返回主 PRD 选择态，原始磁盘文件和既有任务固定输入不删除。资料索引处理中禁止重命名与删除。
- 文件每页 20 项，内容索引每页 25 项。搜索明确显示结果总量和分页，来源全文按 ID 读取。
- 草稿可从本页选择恢复；索引进行中每秒读取实际进度，离开页面不取消后台任务。取消按钮明确结束索引；错误保留资料并可刷新重试。
- 写操作加同步锁，阻止重复点击；请求代次与当前资料 ID 防止迟到结果覆盖。索引期间禁止改输入，取消完成后允许继续添加资料。
- 只有当前版本 ready 时可开始分析；资料变更后索引失效，旧内容不冒充当前版本。
- 系统只读取用户实际上传的文件，不扫描文件中的路径来判断或提示缺件；是否需要补充其他资料由用户决定。索引完成不能推出需求语义完整。
- 主 PRD 选中后的页面按“主文档 → 可选补充 → 下一步 → 明细”排列，内容与操作使用同一 1040px 阅读轴，不再保留居中大标题造成的空白区。
- 主 PRD 选中后显示默认展开的“补充说明与调整”文本区。它允许一次填写背景、范围决定和细化要求，不要求用户逐项选择类型；说明独立保存，不使文件索引失效。
- 准备页只有一个主操作“开始分析”。它以同一次幂等提交固定资料版本与说明版本，自动执行必要的资料读取和索引，然后创建分析任务；用户不需要单独理解或点击索引步骤。
- 用户说明在首次功能识别前进入来源账本。范围决定和整理指令用于组织清单，用户说明不能扩展 PRD 的业务事实；疑问不构成已确认规则。
- 平台对每条用户说明保存应用记录，显示它被判定为何种输入、如何处理以及影响了哪些功能或需求。明确“本期做/不做”的范围决定由程序应用到功能和需求范围；记录与用户原话一起进入交付快照。
- 资料明细与内容索引默认折叠，供核查和处理文件问题；主 PRD、说明和开始动作在 1040px 的同一阅读轴内。目录挂载名称归入目录选项。
- 任务结果可展开查看本次分析说明。结果页继续使用任务级统一输入，只提交用户的调整说明。
- 来源预览不渲染 HTML，使用纯文本；支持 Escape 关闭。所有状态同时用文字表达。

## 仅清单与完整工作流边界

- 正式需求仅保留短文本和原文引用；不展示或生成行为、条件、限制、验收条件四组规格字段。
- 全部需求列表和需求详情不展示旧版逐条检查状态；只保留需求内容、原文入口与本期范围。`ReviewState` 仅用于历史数据和内部协议兼容。
- 导出必须复制冻结原始文件到 sources/files/，缺失时失败；原文位置必须指向该目录中的确切文件。实现先读 PRD，检查清单仅查漏。

- 当前产品边界以 `docs/spec/prd-checklist-only-output.md` 为准：仅输出功能模块、需求清单和出处，不生成待处理事项、建议、主张或独立条件栏目。
- 七阶段执行、九节点 Runtime、任务级调整、局部复核和版本记录保留。平台内部结构、引用和整理校验不进入用户结果页；真实任务执行失败仍在执行记录中显示错误与恢复操作。
- 进度仅表示执行阶段进展：所有阶段成功结束后显示“已完成 · 100%”。只有真实执行失败显示失败状态和恢复操作；不得从内部交付校验派生“平台未完成”等用户任务状态，草稿与正式包仅由产物状态区分。
- 旧版任务仍可只读查看全部需求；展示层从旧 `title/behavior/sourceUnitIds` 读取短标题和来源，不改写持久化数据。
- 交付主文件为 agent-checklist.md，按功能模块分组并使用标准 Markdown 任务复选框；共同原文位置在模块下集中显示，特殊来源随对应条目显示。完成一项将 `- [ ]` 改为 `- [x]`。包内只保留简短 README、原始资料和完整性 manifest，不再生成重复的功能导航、需求快照、解析索引、CSV、Excel 或 pending.json。

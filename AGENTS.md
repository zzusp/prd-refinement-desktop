# AGENTS.md

本文档面向在本仓库中工作的开发者和编码 Agent，说明实现入口、稳定约束和验证方式。面向使用者的产品介绍与操作方法只放在 `README.md`；具体方案和验收证据分别放在 `docs/spec/` 与 `docs/acceptance/`。

## 项目定位

本项目是 Electron 桌面应用，用于将定版 PRD 和补充资料细化为可追溯的功能、需求、约束与待处理事项，再导出供开发和验收使用的结构化交付包。它不读取目标业务项目源码，也不负责直接生成业务代码。

## 代码结构

- `src/`：React 页面、交互和前端类型。
- `electron/`：Electron 主进程、资料解析、分析调度、持久化与交付包生成。
- `tests/`：单元、集成与契约测试。
- `scripts/`：评测、打包冒烟和本地维护脚本。
- `docs/spec/`：实施前的设计与问题分析。
- `docs/acceptance/`：分轮验证记录、证据和验收报告。
- `docs/ops/`：构建、目录治理和运维说明。

## 核心实现约束

1. 主 PRD 决定基础需求范围；补充资料不得在没有明确用户口径时自动覆盖或扩展它。
2. 需求字段、约束和原文明示的验收条件必须绑定冻结输入中的原文证据，不能由模型虚构引用或由程序静默补齐。
3. 功能是组织和导航层，需求明细是业务要求的主要表达；不要把生成的功能摘要当成新的需求来源。
4. 业务澄清与平台整理问题必须分开记录并随相关需求进入交付包。待处理事项不阻止包生成，也不能自动写成确定需求或从实施清单中消失。
5. 分析执行状态、需求交付状态和真实业务实施结果是三件事，不得相互替代。
6. 任务固定资料、用户说明和 Runtime 配置快照；输入或协议变化时创建新任务，不拼接不兼容的旧检查点。
7. 最终产物必须能从功能和需求回查到具体文件、章节、位置及原文。内部来源编号只用于机器数据，不作为用户唯一可见依据。
8. 模型输出必须经过确定性结构、引用和交付条件校验。失败、Runtime 实际上下文超限或未知状态不得静默跳过；本平台不使用估算 token 限制模型调用。Codex 的 JSON 节点应通过 Runtime 的 JSON Schema 参数约束输出结构，不得只靠提示词约定 JSON 语法。
9. “检查通过”不能表述为自然语言语义的 100% 零遗漏，也不能表述为业务代码已完成验收。
10. `requirements.json` 是只读需求快照；`implementation.csv` 是编码 Agent 的逐需求工作清单。平台整理状态、代码实施状态和实际验收状态必须分开记录。
11. 逐功能细化和有据修正的模型协议使用“文本 + evidenceIds”配对对象；模型不得生成 `evidenceBindings`、`sourceUnitIds`、`state` 或字符位置，这些字段由程序在校验证据编号后确定性生成。
12. 所有可达模型节点经 `node-executor.ts` 执行，使用 `model-output-schemas.ts` 中显式 Input / Proposal / Result 契约。领域契约以 Zod 为单一来源；供应商传输差异只能等价转换，不得放宽本地业务约束。模型适配器只返回候选，验收并原子保存成功回执后才推进下游。
13. 节点回执绑定输入、契约版本和实际模型配置，细化成功结果按批保存；失败重试不复用未验收候选。Pipeline 22 不能续用旧协议检查点。取消必须等待旧调用关闭，迟到结果不得提交。
14. `Runtime.executeOperation` 为必需结构化入口；普通 `promptAndWait` 仅用于连接检测。当前 DSH SDK 不支持结构化操作时预检明确失败，不忽略 Schema、不自动切换认证。传输、协议、业务验收和平台异常分别处理，重试计数按工作单元累计。

涉及上述行为变化时，先阅读对应 `docs/spec/`、当前实现和测试，再同步更新用户文档、设计说明与验收证据。

## 常用命令

```powershell
npm install
npm start
npm test
npm run typecheck
npm run build
npm run verify:premium
```

Windows 打包运行 `npm run dist:win`，macOS 打包运行 `npm run dist:mac`。打包和本地运行的临时输出遵循 `docs/ops/artifact-directory-policy.md`，不要在仓库根目录新增构建产物。

## 修改与验证

- 修改前先搜索同类实现和既有测试，保持单一实现路径。
- UI 或业务行为变化需要补充对应测试；涉及真实桌面行为时，除自动测试外还要验证实际 Electron 实例。
- 不把“测试通过”“打包成功”“应用启动”和“业务结果正确”混为同一个结论。
- 需要多轮或高风险验证时，按 `docs/acceptance/<feature>/` 维护用例矩阵、逐轮证据和最终报告。
- 非源码产物必须放入已有的 `docs/` 分类目录，不得散落在仓库根目录或 `docs/` 根目录。

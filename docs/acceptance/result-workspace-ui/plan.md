# 结果工作台显示修正验收计划

验证四个用户问题：执行校验命名与层级、旧任务全部需求白屏、表格复选框对齐、运行统计区布局。实现方案见 `docs/spec/result-workspace-ui-fixes.md`。

使用组件测试验证展示契约，使用真实 Pipeline 22 任务 `T-430555BA` 的隔离副本验证旧数据读取和实际 Electron 布局。截图及完整 profile 仅保存在 `docs/tmp/result-workspace-ui/`，不进入 Git。
